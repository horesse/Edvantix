edition = "2023";

option csharp_namespace = "Edvantix.Organizational.Grpc.Services";

package permission.v1;

service PermissionGrpcService {
  rpc CheckPermission (CheckPermissionRequest) returns (CheckPermissionResponse);
}

message CheckPermissionRequest {
  string organization_id = 1;
  string permission = 2;
  // Идентификатор профиля, для которого выполняется проверка.
  // Передаётся явно, так как токен по gRPC не пробрасывается.
  string profile_id = 3;
}

message CheckPermissionResponse {
  bool has_permission = 1;
}
