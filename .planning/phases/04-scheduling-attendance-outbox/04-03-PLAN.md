---
phase: 04-scheduling-attendance-outbox
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceQuery.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceQueryHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/AttendanceRecordDto.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceEndpoint.cs
autonomous: true
requirements:
  - SCH-08
must_haves:
  truths:
    - "GET /slots/{slotId}/attendance returns list of attendance records for the slot"
    - "Each record in response includes studentId, status (string), correlationId, markedAt"
    - "Endpoint is protected by scheduling.view-schedule permission"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceQueryHandler.cs"
      provides: "Query handler returning attendance DTOs"
      contains: "GetBySlotAsync"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/AttendanceRecordDto.cs"
      provides: "DTO with string Status"
      contains: "string Status"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceEndpoint.cs"
      provides: "GET endpoint mapped to query"
      contains: "ViewSchedule"
  key_links:
    - from: "GetSlotAttendanceQueryHandler.cs"
      to: "IAttendanceRecordRepository"
      via: "GetBySlotAsync call"
      pattern: "repository.GetBySlotAsync"
    - from: "GetSlotAttendanceEndpoint.cs"
      to: "GetSlotAttendanceQuery"
      via: "Mediator send"
      pattern: "new GetSlotAttendanceQuery"
---

<objective>
Create the GetSlotAttendance query (GET endpoint) that returns attendance records for a lesson slot.

Purpose: Completes the read side of SCH-08 attendance marking. Managers and teachers can view attendance records for any slot they have permission to see.
Output: Working GET endpoint returning attendance records as DTOs.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling-attendance-outbox/04-CONTEXT.md
@.planning/phases/04-scheduling-attendance-outbox/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From IAttendanceRecordRepository.cs (created in Plan 01):
```csharp
public interface IAttendanceRecordRepository : IRepository<AttendanceRecord>
{
    Task<AttendanceRecord?> FindBySlotAndStudentAsync(Guid slotId, Guid studentId, CancellationToken ct);
    Task<IReadOnlyList<AttendanceRecord>> GetBySlotAsync(Guid slotId, CancellationToken ct);
    void Add(AttendanceRecord record);
}
```

From AttendanceRecord.cs (created in Plan 01):
```csharp
public sealed class AttendanceRecord : Entity, IAggregateRoot, ITenanted
{
    public Guid StudentId { get; private set; }
    public AttendanceStatus Status { get; private set; }
    public Guid CorrelationId { get; private set; }
    public DateTimeOffset MarkedAt { get; private set; }
}
```

From GetScheduleQuery.cs / GetScheduleQueryHandler.cs (query pattern to replicate):
- Read these files to replicate the query + handler + endpoint pattern with Mediator IQuery<T>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GetSlotAttendance query, handler, DTO, and endpoint</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceQuery.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceQueryHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/AttendanceRecordDto.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/GetSlotAttendance/GetSlotAttendanceEndpoint.cs
  </files>
  <read_first>
    src/Services/Scheduling/Edvantix.Scheduling/Features/Schedule/GetSchedule/GetScheduleQuery.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Schedule/GetSchedule/GetScheduleQueryHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Schedule/GetSchedule/GetScheduleEndpoint.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/AttendanceRecord.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/IAttendanceRecordRepository.cs
  </read_first>
  <action>
    1. Create `AttendanceRecordDto.cs`:
       - `public sealed record AttendanceRecordDto(Guid StudentId, string Status, Guid CorrelationId, DateTimeOffset MarkedAt);`
       - `Status` as string (per D-07 response shape)

    2. Create `GetSlotAttendanceQuery.cs`:
       - `public sealed record GetSlotAttendanceQuery(Guid SlotId) : IQuery<IReadOnlyList<AttendanceRecordDto>>;`
       - Name starts with `Get` per arch rule.

    3. Create `GetSlotAttendanceQueryHandler.cs`:
       - Inject `IAttendanceRecordRepository`
       - Call `repository.GetBySlotAsync(query.SlotId, ct)`
       - Map each `AttendanceRecord` to `AttendanceRecordDto(r.StudentId, r.Status.ToString(), r.CorrelationId, r.MarkedAt)`
       - Return as `IReadOnlyList<AttendanceRecordDto>`

    4. Create `GetSlotAttendanceEndpoint.cs`:
       - `GET /v1/slots/{slotId}/attendance` per D-07
       - Maps route param to `GetSlotAttendanceQuery(slotId)`
       - Returns 200 with list of `AttendanceRecordDto`
       - Authorization: `.RequireAuthorization(SchedulingPermissions.ViewSchedule)` per D-09 (reuses existing permission)
       - Follow the existing endpoint pattern from GetScheduleEndpoint.cs
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling && dotnet test tests/Edvantix.ArchTests -x</automated>
  </verify>
  <done>GET /v1/slots/{slotId}/attendance endpoint exists, compiles, and passes arch tests. Returns list of AttendanceRecordDto with string Status. Protected by ViewSchedule permission per D-09.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling` compiles without errors
2. `dotnet test tests/Edvantix.ArchTests -x` all arch tests pass (query name starts with Get)
3. `just test` full solution test suite green
</verification>

<success_criteria>
- GET /v1/slots/{slotId}/attendance endpoint exists with ViewSchedule authorization
- AttendanceRecordDto contains string Status, Guid CorrelationId, DateTimeOffset MarkedAt
- Query handler maps from AttendanceRecord to AttendanceRecordDto using Status.ToString()
- All arch tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-attendance-outbox/04-03-SUMMARY.md`
</output>
