---
phase: 03-scheduling-slots-and-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs
  - src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs
  - src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj
  - src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Program.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContextFactory.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Configurations/SchedulingAppSettings.cs
  - src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Properties/launchSettings.json
  - src/Services/Scheduling/Edvantix.Scheduling/appsettings.json
  - src/Services/Scheduling/Edvantix.Scheduling/appsettings.Development.json
autonomous: true
requirements: [SCH-10]

must_haves:
  truths:
    - "Scheduling service compiles and is visible as a resource in Aspire dashboard"
    - "Scheduling permissions are seeded to Organizations via HTTP on startup"
    - "All 4 scheduling permission constants defined in SchedulingPermissions class"
    - "SchedulingDbContext exists with empty DbSets (domain entities added in Plan 02)"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj"
      provides: "Scheduling service project"
    - path: "src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs"
      provides: "Permission string constants — scheduling.* namespace ONLY (4 permissions)"
      contains: "scheduling.create-lesson-slot"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs"
      provides: "DbContext shell (HasQueryFilter added in Plan 02)"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs"
      provides: "Permission seeder via Organizations HTTP"
      contains: "IHttpClientFactory"
  key_links:
    - from: "src/Aspire/Edvantix.AppHost/AppHost.cs"
      to: "src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj"
      via: "AddProject<Edvantix_Scheduling>"
      pattern: "AddProject.*Scheduling"
    - from: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs"
      to: "Organizations HTTP POST /v1/permissions/register"
      via: "IHttpClientFactory named client"
      pattern: "permissions/register"
---

<objective>
Bootstrap the Scheduling service project, constants, Aspire wiring, Keycloak client, and permission seeder.

Purpose: Create the service skeleton and infrastructure so that Plan 02 can add the LessonSlot domain entity and Plans 04-05 can build features on top.
Output: Compiling Scheduling service visible in Aspire dashboard; permission constants in Edvantix.Constants; Keycloak client entry; empty DbContext shell.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-slots-and-views/03-CONTEXT.md
@.planning/phases/03-scheduling-slots-and-views/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Scheduling service project, constants, settings, and Program.cs</name>
  <files>
    src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs,
    src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj,
    src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Program.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContextFactory.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Configurations/SchedulingAppSettings.cs,
    src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Properties/launchSettings.json,
    src/Services/Scheduling/Edvantix.Scheduling/appsettings.json,
    src/Services/Scheduling/Edvantix.Scheduling/appsettings.Development.json
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Edvantix.Organizations.csproj,
    src/Services/Organizations/Edvantix.Organizations/Program.cs,
    src/Services/Organizations/Edvantix.Organizations/Extensions/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/OrganizationsDbContext.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/OrganizationsDbContextFactory.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Seeding/PermissionSeeder.cs,
    src/Services/Organizations/Edvantix.Organizations/Configurations/OrganizationsAppSettings.cs,
    src/Services/Organizations/Edvantix.Organizations/GlobalUsings.cs,
    src/Services/Organizations/Edvantix.Organizations/IOrganizationsApiMarker.cs,
    src/Services/Organizations/Edvantix.Organizations/Properties/launchSettings.json,
    src/Services/Organizations/Edvantix.Organizations/appsettings.json,
    src/Services/Organizations/Edvantix.Organizations/appsettings.Development.json,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs,
    src/BuildingBlocks/Edvantix.Constants/Permissions/OrganizationsPermissions.cs
  </read_first>
  <action>
**Constants (per D-11, D-12 — REVISED: only 4 scheduling permissions):**

1. Add to `Services.cs`:
```csharp
public static readonly string Scheduling = nameof(Scheduling).ToLowerInvariant();
```

2. Add to `Components.Database`:
```csharp
public static readonly string Scheduling = $"{nameof(Scheduling).ToLowerInvariant()}{Suffix}";
```

3. Create `SchedulingPermissions.cs` in `Edvantix.Constants/Permissions/` following `OrganizationsPermissions.cs` pattern. Permission strings (per D-12 REVISED — group permissions moved to GroupsPermissions in Plan 03-07):
- `scheduling.create-lesson-slot`
- `scheduling.edit-lesson-slot`
- `scheduling.delete-lesson-slot`
- `scheduling.view-schedule`

Include `public static IReadOnlyList<string> All => [...]` collecting all 4 constants.

IMPORTANT: Do NOT include any `scheduling.create-group`, `scheduling.update-group`, `scheduling.delete-group`, or `scheduling.manage-group-membership` — those are now `groups.*` permissions in GroupsPermissions (Plan 03-07).

**Scheduling service project:**

4. Create `Edvantix.Scheduling.csproj` mirroring `Edvantix.Organizations.csproj` exactly:
- Same `Sdk="Microsoft.NET.Sdk.Web"`, same `SuppressCheckGrpcNetClientFactoryVersion`, same `InternalsVisibleTo` for `DynamicProxyGenAssembly2` and `$(AssemblyName).UnitTests`
- Same PackageReferences: `Grpc.Tools`, `Grpc.AspNetCore.Server.ClientFactory`, `MassTransit.EntityFrameworkCore`, `Mediator.SourceGenerator`, `Microsoft.EntityFrameworkCore.Tools`
- ProjectReference to `Edvantix.ServiceDefaults`
- Protobuf entry: `<Protobuf Include="Grpc\Protos\persona\v1\profile.proto" GrpcServices="Client" />`

5. Create `ISchedulingApiMarker.cs`: empty marker interface `public interface ISchedulingApiMarker;` in namespace `Edvantix.Scheduling`.

6. Create `Program.cs` following Organizations pattern exactly:
- `builder.WebHost.ConfigureKestrel(o => o.AddServerHeader = false);`
- `builder.AddServiceDefaults(); builder.AddApplicationServices();`
- Middleware pipeline: `UseExceptionHandler`, `UseStatusCodePages`, `UseDefaultCors`, `UseKeycloakTokenIntrospection`, `UseRateLimiter`, `UseTenantContext` (BEFORE UseAuthorization)
- `app.NewApiVersionSet()` with `ApiVersions.V1`
- `app.MapEndpoints(apiVersionSet);`
- `app.MapDefaultEndpoints(); app.UseDefaultOpenApi(); app.UseAuthorization(); app.Run();`
- NO `MapGrpcService` (Scheduling is a gRPC CLIENT, not server)

7. Create `Extensions/Extensions.cs` (`AddApplicationServices`) following Organizations pattern:
- `AddDefaultCors`, `AddDefaultAuthentication().WithKeycloakClaimsTransformation()`
- Authorization: default policy with `RequireAuthenticatedUser().RequireScope($"{Services.Scheduling}_{Authorization.Actions.Read}")`
- `AddPermissionAuthorization(...)` pointing at `Services.Organizations` URL
- Add authorization policies for each scheduling permission using `.AddPolicy(SchedulingPermissions.CreateLessonSlot, p => p.RequirePermission(SchedulingPermissions.CreateLessonSlot))` and repeat for all 4 permissions
- `AddTenantContext`, exception handlers, `AddMediator`, `AddAppSettings<SchedulingAppSettings>`, `AddRateLimiting`
- `AddPersistenceServices`, validators from `ISchedulingApiMarker`, `ClaimsPrincipal` transient, activity/metrics
- `AddVersioning`, `AddEndpoints(typeof(ISchedulingApiMarker))`, `AddDefaultOpenApi` with `OpenApiInfoDefinitionsTransformer<SchedulingAppSettings>`
- `AddEventBus` with `AddEntityFrameworkOutbox<SchedulingDbContext>` (wire Kafka now for Phase 4)
- `AddKeycloakTokenIntrospection`
- `AddHostedService<PermissionSeeder>()`
- Register named HttpClient for Organizations: `services.AddHttpClient("organizations", c => c.BaseAddress = new Uri($"http://{Services.Organizations}"))`
- Do NOT include `AddMapper`/`AddEventDispatcher`/`AddScoped<IEventMapper>` yet (no domain events in Phase 3)

8. Create `Infrastructure/Extensions.cs` (`AddPersistenceServices`) mirroring Organizations:
```csharp
builder.AddAzurePostgresDbContext<SchedulingDbContext>(
    Components.Database.Scheduling,
    _ => { services.AddMigration<SchedulingDbContext>(); services.AddRepositories(typeof(ISchedulingApiMarker)); }
);
```

9. Create `SchedulingDbContext` shell mirroring `OrganizationsDbContext`:
- Primary constructor: `(DbContextOptions<SchedulingDbContext> options, ITenantContext tenantContext)`
- Implements `IUnitOfWork`
- Empty `OnModelCreating` for now (just `base.OnModelCreating`, `AddInboxStateEntity`, `AddOutboxMessageEntity`, `AddOutboxStateEntity`, `ApplyConfigurationsFromAssembly`)
- `SaveEntitiesAsync` identical to Organizations
- NOTE: DbSets and HasQueryFilter calls will be added in Plan 02 when LessonSlot entity is created

10. Create `SchedulingDbContextFactory` mirroring `OrganizationsDbContextFactory`.

11. Create `PermissionSeeder.cs` as `IHostedService` using `IHttpClientFactory` (NOT `IPermissionRepository`):
- Inject `IHttpClientFactory` and `ILogger<PermissionSeeder>`
- `StartAsync`: create client via `httpClientFactory.CreateClient("organizations")`, POST to `/v1/permissions/register` with body `{ Names = SchedulingPermissions.All }`, call `response.EnsureSuccessStatusCode()`
- Add retry with Polly for startup ordering resilience

12. Create `SchedulingAppSettings.cs` mirroring `OrganizationsAppSettings.cs`.

13. Create `GlobalUsings.cs` with base namespace imports (aggregate namespaces added in Plan 02).

14. Create `Properties/launchSettings.json`, `appsettings.json`, `appsettings.Development.json` mirroring Organizations (different ports).
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj</automated>
  </verify>
  <done>Scheduling service project compiles. Constants for service name, database, and 4 permissions exist. SchedulingDbContext shell exists. PermissionSeeder uses HTTP to Organizations.</done>
</task>

<task type="auto">
  <name>Task 2: Aspire wiring, Keycloak client, and gRPC Persona client setup</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/IPersonaProfileService.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/PersonaProfileService.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Protos/persona/v1/profile.proto,
    src/Aspire/Edvantix.AppHost/AppHost.cs,
    src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Grpc/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Services/IPersonaProfileService.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Services/PersonaProfileService.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Protos/persona/v1/profile.proto,
    src/Aspire/Edvantix.AppHost/AppHost.cs,
    src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json,
    src/Aspire/Edvantix.AppHost/Extensions/Security/KeycloakExtensions.cs
  </read_first>
  <action>
**gRPC Persona client (same as Organizations):**

1. Copy `Grpc/Protos/persona/v1/profile.proto` from Organizations.

2. Create `Grpc/Extensions.cs` (`AddGrpcServices`) mirroring Organizations gRPC client wiring.

3. Create `Grpc/Services/IPersonaProfileService.cs` and `PersonaProfileService.cs` mirroring Organizations.

4. Add `AddGrpcServices` call to `Extensions/Extensions.cs` `AddApplicationServices` method (from Task 1).

**Aspire wiring (per D-13):**

5. In `AppHost.cs`, add after `organizationsDb` line:
```csharp
var schedulingDb = postgres.AddDatabase(Components.Database.Scheduling);
```
Add after `organizationsApi` block:
```csharp
var schedulingApi = builder
    .AddProject<Edvantix_Scheduling>(Services.Scheduling)
    .WithReference(schedulingDb)
    .WaitFor(schedulingDb)
    .WithKeycloak(keycloak)
    .WaitFor(keycloak)
    .WithReference(personaApi)
    .WaitFor(personaApi)
    .WithReference(organizationsApi)
    .WaitFor(organizationsApi)
    .WithReference(queue)
    .WaitFor(queue)
    .WithContainerRegistry(registry)
    .WithFriendlyUrls();
```
Add to gateway: `.WithService(schedulingApi, true)` in the `AddApiGatewayProxy()` chain.
Add to Scalar: `.WithOpenAPI(schedulingApi)` in the `AddScalar` block.

**Keycloak (per D-14):**

6. In `EdvantixRealms.json`, add a new client entry for `${CLIENT_SCHEDULING_ID}` following the exact structure of the `${CLIENT_PERSONA_ID}` client entry (copy the Persona client block, replace all `PERSONA` with `SCHEDULING`, generate new UUIDs for `id` fields). Include `service_account` in defaultClientScopes. Also add the corresponding `service-account-${CLIENT_SCHEDULING_ID}` user entry, `protocolMappers` section, and `clientScopeMappings` entry following Persona's exact pattern.
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj --no-restore && dotnet build src/Aspire/Edvantix.AppHost/Edvantix.AppHost.csproj --no-restore</automated>
  </verify>
  <done>Aspire AppHost registers schedulingDb + schedulingApi with all dependencies (Keycloak, Persona, Organizations, Kafka). Keycloak realm includes Scheduling client. Persona gRPC client wired into Scheduling service. Gateway includes scheduling route. Scalar dashboard includes scheduling OpenAPI.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` succeeds
2. `dotnet build src/Aspire/Edvantix.AppHost/Edvantix.AppHost.csproj` succeeds
3. `grep -c "scheduling\." src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs` returns 4
</verification>

<success_criteria>
- Scheduling service compiles as an independent project
- Aspire AppHost.cs registers schedulingDb + schedulingApi with Keycloak, Persona, Organizations, Kafka references
- Keycloak realm JSON includes Scheduling client entry
- All 4 scheduling permission constants defined (no group permissions — those are in GroupsPermissions)
- PermissionSeeder uses HTTP to Organizations (not direct DB)
- SchedulingDbContext shell compiles (domain entities added in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-slots-and-views/03-01-SUMMARY.md`
</output>
