edition = "2023";

option csharp_namespace = "Edvantix.Persona.Grpc.Services";

package profile.v1;

// Сервис профилей — используется другими микросервисами для получения данных пользователей.
service ProfileGrpcService {
  // Возвращает краткий профиль по внутреннему ID или AccountId (Keycloak GUID).
  // Ровно одно поле из oneof identifier должно быть заполнено.
  rpc GetProfile (GetProfileRequest) returns (ProfileReply);
}

message GetProfileRequest {
  oneof identifier {
    uint64 profile_id = 1;
    string account_id = 2;
  }
}

message ProfileReply {
  uint64 id = 1;
  string account_id = 2;
  int32 gender = 3;
  string birth_date = 4;
  string full_name = 5;
  string first_name = 6;
  string last_name = 7;
  string middle_name = 8;
  string login = 9;
}
