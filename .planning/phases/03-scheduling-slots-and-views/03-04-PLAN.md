---
phase: 03-scheduling-slots-and-views
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommand.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandValidator.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupEndpoint.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommand.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommandHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupEndpoint.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersQuery.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersQueryHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersEndpoint.cs
autonomous: true
requirements: [SCH-06, SCH-07]

must_haves:
  truths:
    - "Manager can add a student to a group (SCH-06)"
    - "Manager can remove a student from a group (SCH-07)"
    - "Duplicate student-group membership is silently ignored (no error, no duplicate row)"
    - "Student profile is validated via Persona gRPC before adding to group"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs"
      provides: "Add student to group with Persona validation"
      contains: "IPersonaProfileService"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommandHandler.cs"
      provides: "Remove student from group"
      contains: "RemoveMember"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupEndpoint.cs"
      provides: "POST /groups/{groupId}/members endpoint"
      contains: "RequireAuthorization"
  key_links:
    - from: "AddStudentToGroupCommandHandler"
      to: "IPersonaProfileService"
      via: "gRPC call to validate student profile exists"
      pattern: "IPersonaProfileService"
    - from: "AddStudentToGroupCommandHandler"
      to: "Group.AddMember"
      via: "aggregate method call"
      pattern: "AddMember"
    - from: "RemoveStudentFromGroupCommandHandler"
      to: "Group.RemoveMember"
      via: "aggregate method call"
      pattern: "RemoveMember"
---

<objective>
Implement group membership management — add and remove students from groups with profile validation.

Purpose: Deliver SCH-06 (add student to group) and SCH-07 (remove student from group) so that students can be enrolled in groups and their membership is reflected in the schedule view (Plan 03).
Output: POST /groups/{groupId}/members and DELETE /groups/{groupId}/members/{studentId} endpoints; GET /groups/{groupId}/members for listing.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scheduling-slots-and-views/03-CONTEXT.md
@.planning/phases/03-scheduling-slots-and-views/03-RESEARCH.md
@.planning/phases/03-scheduling-slots-and-views/03-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan depends on -->

From IGroupRepository.cs:
```csharp
public interface IGroupRepository : IRepository<Group>
{
    Task<Group?> FindByIdAsync(Guid groupId, CancellationToken ct);
    Task<Group?> FindByIdWithMembersAsync(Guid groupId, CancellationToken ct);
    void Add(Group group);
}
```

From Group.cs:
```csharp
public sealed class Group : Entity, IAggregateRoot, ISoftDelete, ITenanted
{
    public IReadOnlyCollection<GroupMembership> Members => _members.AsReadOnly();

    public void AddMember(Guid studentId, DateTimeOffset joinedAt)
    {
        Guard.Against.Default(studentId, nameof(studentId));
        if (_members.Any(m => m.StudentId == studentId)) return; // idempotent
        _members.Add(new GroupMembership(Id, SchoolId, studentId, joinedAt));
    }

    public void RemoveMember(Guid studentId)
    {
        var existing = _members.FirstOrDefault(m => m.StudentId == studentId);
        if (existing is not null) _members.Remove(existing);
    }
}
```

From GroupMembership.cs:
```csharp
public sealed class GroupMembership : Entity, ITenanted
{
    public Guid GroupId { get; private set; }
    public Guid SchoolId { get; private set; }
    public Guid StudentId { get; private set; }
    public DateTimeOffset JoinedAt { get; private set; }
}
```

From IPersonaProfileService.cs (Scheduling Grpc wrapper):
```csharp
public interface IPersonaProfileService
{
    Task<ProfileResponse?> GetProfileAsync(Guid profileId, CancellationToken ct);
}
```

From SchedulingPermissions.cs:
```csharp
public const string ManageGroupMembership = "scheduling.manage-group-membership";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add student to group command with Persona profile validation</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommand.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandValidator.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupEndpoint.cs
  </files>
  <read_first>
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/IPersonaProfileService.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/PersonaProfileService.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/AssignRole/AssignRoleCommand.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/AssignRole/AssignRoleCommandHandler.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/AssignRole/AssignRoleEndpoint.cs,
    src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs
  </read_first>
  <action>
Follow the Organizations `AssignRole` pattern which also validates a profile via Persona gRPC before creating an assignment.

**AddStudentToGroupCommand:**
```csharp
public sealed record AddStudentToGroupCommand(
    Guid GroupId,
    Guid StudentId
) : ICommand<Unit>;
```

**AddStudentToGroupCommandHandler:**
Inject: `IGroupRepository`, `IPersonaProfileService`, `IUnitOfWork`

1. Validate student profile exists via Persona gRPC: `var profile = await personaService.GetProfileAsync(command.StudentId, ct)`. If `profile is null`, throw `NotFoundException` with message `$"Profile {command.StudentId} not found"`
2. Load group with members: `var group = await groupRepo.FindByIdWithMembersAsync(command.GroupId, ct)`. If `group is null`, throw `NotFoundException`
3. Call `group.AddMember(command.StudentId, DateTimeHelper.UtcNow())` — this is idempotent (Group.AddMember silently returns if duplicate per domain model)
4. `await unitOfWork.SaveEntitiesAsync(ct)`

Use `DateTimeHelper.UtcNow()` from Chassis (NOT `DateTimeOffset.UtcNow`) per SCH-10 convention.

**AddStudentToGroupCommandValidator:**
- `GroupId` must not be default
- `StudentId` must not be default

**AddStudentToGroupEndpoint:**
- POST `/groups/{groupId}/members`
- Request body: `{ "studentId": "guid" }`
- Construct command from route `groupId` + body `studentId`
- `.Produces(StatusCodes.Status204NoContent)`
- `.ProducesValidationProblem()`
- `.ProducesProblem(StatusCodes.Status404NotFound)`
- `.WithName("AddStudentToGroup").WithTags("GroupMembership").MapToApiVersion(ApiVersions.V1)`
- `.RequireAuthorization(SchedulingPermissions.ManageGroupMembership)`
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs` contains `IPersonaProfileService` and `GetProfileAsync` and `AddMember` and `DateTimeHelper.UtcNow`
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs` contains `FindByIdWithMembersAsync`
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupEndpoint.cs` contains `RequireAuthorization(SchedulingPermissions.ManageGroupMembership)` and `groups/{groupId}/members`
    - `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` exits 0
  </acceptance_criteria>
  <done>POST /groups/{groupId}/members endpoint compiles. Handler validates student profile via Persona gRPC, loads group with members, calls group.AddMember (idempotent), saves. Protected by scheduling.manage-group-membership permission. Uses DateTimeHelper.UtcNow() for JoinedAt.</done>
</task>

<task type="auto">
  <name>Task 2: Remove student from group command and list group members query</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommand.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommandHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupEndpoint.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersQuery.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersQueryHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersEndpoint.cs
  </files>
  <read_first>
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommand.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupCommandHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/AddStudentToGroup/AddStudentToGroupEndpoint.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/RevokeRole/RevokeRoleCommand.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/RevokeRole/RevokeRoleCommandHandler.cs,
    src/Services/Organizations/Edvantix.Organizations/Features/UserRoleAssignment/RevokeRole/RevokeRoleEndpoint.cs
  </read_first>
  <action>
**RemoveStudentFromGroupCommand:**
```csharp
public sealed record RemoveStudentFromGroupCommand(
    Guid GroupId,
    Guid StudentId
) : ICommand<Unit>;
```

**RemoveStudentFromGroupCommandHandler:**
Inject: `IGroupRepository`, `IUnitOfWork`

1. Load group with members: `var group = await groupRepo.FindByIdWithMembersAsync(command.GroupId, ct)`. If `group is null`, throw `NotFoundException`
2. Call `group.RemoveMember(command.StudentId)` — silently no-ops if member not found (same idempotent pattern as AddMember)
3. `await unitOfWork.SaveEntitiesAsync(ct)`

**RemoveStudentFromGroupEndpoint:**
- DELETE `/groups/{groupId}/members/{studentId}`
- Construct command from route parameters `groupId` and `studentId`
- `.Produces(StatusCodes.Status204NoContent)`
- `.ProducesProblem(StatusCodes.Status404NotFound)`
- `.WithName("RemoveStudentFromGroup").WithTags("GroupMembership").MapToApiVersion(ApiVersions.V1)`
- `.RequireAuthorization(SchedulingPermissions.ManageGroupMembership)`

**GetGroupMembersQuery:**
```csharp
public sealed record GetGroupMembersQuery(Guid GroupId) : IQuery<List<GroupMemberDto>>;
```

**GroupMemberDto:**
```csharp
public sealed record GroupMemberDto(
    Guid StudentId,
    DateTimeOffset JoinedAt
);
```

**GetGroupMembersQueryHandler:**
Inject: `IGroupRepository`

1. Load group with members: `var group = await groupRepo.FindByIdWithMembersAsync(query.GroupId, ct)`. If `group is null`, throw `NotFoundException`
2. Map `group.Members` to `List<GroupMemberDto>`: `group.Members.Select(m => new GroupMemberDto(m.StudentId, m.JoinedAt)).ToList()`
3. Return the list

**GetGroupMembersEndpoint:**
- GET `/groups/{groupId}/members`
- `.Produces<List<GroupMemberDto>>(200)`
- `.ProducesProblem(StatusCodes.Status404NotFound)`
- `.WithName("GetGroupMembers").WithTags("GroupMembership").MapToApiVersion(ApiVersions.V1)`
- `.RequireAuthorization(SchedulingPermissions.ManageGroupMembership)`
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupCommandHandler.cs` contains `FindByIdWithMembersAsync` and `RemoveMember` and `SaveEntitiesAsync`
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/RemoveStudentFromGroup/RemoveStudentFromGroupEndpoint.cs` contains `groups/{groupId}/members/{studentId}` and `RequireAuthorization(SchedulingPermissions.ManageGroupMembership)`
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersEndpoint.cs` contains `groups/{groupId}/members` and `RequireAuthorization`
    - `src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/GetGroupMembers/GetGroupMembersQueryHandler.cs` contains `FindByIdWithMembersAsync` and `GroupMemberDto`
    - `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` exits 0
  </acceptance_criteria>
  <done>DELETE /groups/{groupId}/members/{studentId} removes a student from a group (idempotent). GET /groups/{groupId}/members returns list of GroupMemberDto with StudentId and JoinedAt. Both protected by scheduling.manage-group-membership permission. All operations go through Group aggregate root's AddMember/RemoveMember methods.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` succeeds
2. `dotnet test tests/Edvantix.ArchTests` passes
3. `grep -r "DateTime\.Now\|DateTime\.UtcNow" src/Services/Scheduling/Edvantix.Scheduling/Features/GroupMembership/` returns no matches
</verification>

<success_criteria>
- POST /groups/{groupId}/members adds student with Persona profile validation
- DELETE /groups/{groupId}/members/{studentId} removes student
- GET /groups/{groupId}/members lists current members
- AddMember uses DateTimeHelper.UtcNow() for JoinedAt timestamp
- All endpoints protected by scheduling.manage-group-membership
- Duplicate membership silently ignored (Group.AddMember idempotent)
- Missing member removal silently ignored (Group.RemoveMember no-ops)
- Architecture tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-slots-and-views/03-04-SUMMARY.md`
</output>
