---
phase: 04-scheduling-attendance-outbox
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EventServices/Events/AttendanceRecordedEvent.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EventServices/EventMapper.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/EventHandlers/AttendanceRecordedDomainEventHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/IntegrationEvents/AttendanceRecordedIntegrationEvent.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/AttendanceRecord.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommand.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommandHandler.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommandValidator.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceEndpoint.cs
  - tests/Edvantix.Scheduling.UnitTests/Infrastructure/EventMapperTests.cs
  - tests/Edvantix.Scheduling.UnitTests/Features/MarkAttendanceCommandHandlerTests.cs
autonomous: true
requirements:
  - SCH-08
  - SCH-09
must_haves:
  truths:
    - "PUT /slots/{slotId}/attendance/{studentId} creates a new AttendanceRecord when none exists"
    - "PUT /slots/{slotId}/attendance/{studentId} updates existing record when one exists (upsert)"
    - "AttendanceRecordedEvent domain event is raised on both create and update"
    - "EventMapper maps AttendanceRecordedEvent to AttendanceRecordedIntegrationEvent with all 6 fields including CorrelationId"
    - "Status is sent as string in integration event, not int"
    - "IEventMapper and AddEventDispatcher() are registered in Extensions.cs DI"
    - "Integration event reaches outbox table after SaveEntitiesAsync"
    - "Kafka topic is attendance-recorded-integration-event (auto-derived per D-11)"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommandHandler.cs"
      provides: "Upsert handler using EF change tracking"
      contains: "FindBySlotAndStudentAsync"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EventServices/EventMapper.cs"
      provides: "Domain-to-integration event mapping"
      contains: "AttendanceRecordedEvent"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/IntegrationEvents/AttendanceRecordedIntegrationEvent.cs"
      provides: "Integration event for Kafka outbox"
      contains: "namespace Edvantix.Contracts"
    - path: "tests/Edvantix.Scheduling.UnitTests/Infrastructure/EventMapperTests.cs"
      provides: "EventMapper unit tests"
      min_lines: 30
  key_links:
    - from: "AttendanceRecord.cs"
      to: "AttendanceRecordedEvent"
      via: "RegisterDomainEvent in constructor and UpdateStatus"
      pattern: "RegisterDomainEvent.*AttendanceRecordedEvent"
    - from: "EventMapper.cs"
      to: "AttendanceRecordedIntegrationEvent"
      via: "switch expression mapping"
      pattern: "AttendanceRecordedEvent e => new AttendanceRecordedIntegrationEvent"
    - from: "Extensions.cs"
      to: "EventMapper"
      via: "DI registration"
      pattern: "AddScoped<IEventMapper, EventMapper>"
    - from: "Extensions.cs"
      to: "EventDispatcher"
      via: "DI registration"
      pattern: "AddEventDispatcher"
---

<objective>
Wire the domain event pipeline (AttendanceRecordedEvent -> EventMapper -> AttendanceRecordedIntegrationEvent -> outbox -> Kafka) and create the MarkAttendance command (PUT endpoint with upsert). Complete all unit tests for EventMapper and handler.

Purpose: Completes SCH-08 (attendance marking) and SCH-09 (integration event via outbox). After this plan, attendance marking produces a Kafka event on the `attendance-recorded-integration-event` topic that Payments (Phase 5) can consume.
Output: Working PUT endpoint, domain event pipeline, integration event in outbox, all tests green.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling-attendance-outbox/04-CONTEXT.md
@.planning/phases/04-scheduling-attendance-outbox/04-RESEARCH.md
@.planning/phases/04-scheduling-attendance-outbox/04-VALIDATION.md
@.planning/phases/04-scheduling-attendance-outbox/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From AttendanceRecord.cs (created in Plan 01):
```csharp
public sealed class AttendanceRecord : Entity, IAggregateRoot, ITenanted
{
    public Guid SchoolId { get; private set; }
    public Guid LessonSlotId { get; private set; }
    public Guid StudentId { get; private set; }
    public AttendanceStatus Status { get; private set; }
    public Guid CorrelationId { get; private set; }
    public DateTimeOffset MarkedAt { get; private set; }
    // Constructor: AttendanceRecord(Guid schoolId, Guid lessonSlotId, Guid studentId, AttendanceStatus status)
    // Method: void UpdateStatus(AttendanceStatus newStatus)
}
```

From IAttendanceRecordRepository.cs (created in Plan 01):
```csharp
public interface IAttendanceRecordRepository : IRepository<AttendanceRecord>
{
    Task<AttendanceRecord?> FindBySlotAndStudentAsync(Guid slotId, Guid studentId, CancellationToken ct);
    Task<IReadOnlyList<AttendanceRecord>> GetBySlotAsync(Guid slotId, CancellationToken ct);
    void Add(AttendanceRecord record);
}
```

From Organizations EventMapper.cs (pattern to replicate):
```csharp
internal sealed class EventMapper : IEventMapper
{
    public IntegrationEvent MapToIntegrationEvent(DomainEvent @event)
    {
        return @event switch
        {
            UserRoleAssignedEvent e => new UserPermissionsInvalidatedIntegrationEvent(...),
            _ => throw new ArgumentOutOfRangeException(nameof(@event), @event, null),
        };
    }
}
```

From Organizations Domain/EventHandlers/ (pattern to replicate):
```csharp
public sealed class UserRoleAssignedEventHandler(IEventDispatcher dispatcher)
    : INotificationHandler<UserRoleAssignedEvent>
{
    public async ValueTask Handle(UserRoleAssignedEvent notification, CancellationToken ct)
        => await dispatcher.DispatchAsync(notification, ct);
}
```

From Organizations Extensions.cs lines 117-118 (DI to replicate):
```csharp
services.AddScoped<IEventMapper, EventMapper>();
services.AddEventDispatcher();
```

From UserPermissionsInvalidatedIntegrationEvent.cs (integration event pattern):
```csharp
namespace Edvantix.Contracts;
public sealed record UserPermissionsInvalidatedIntegrationEvent(
    Guid? UserId, Guid SchoolId, DateTimeOffset Timestamp
) : IntegrationEvent;
```

From UserRoleAssignedEvent.cs (domain event pattern):
```csharp
public sealed class UserRoleAssignedEvent(Guid profileId, Guid schoolId, Guid roleId) : DomainEvent
{
    public Guid ProfileId { get; } = profileId;
    public Guid SchoolId { get; } = schoolId;
    public Guid RoleId { get; } = roleId;
}
```

From CreateLessonSlotEndpoint.cs / CreateLessonSlotCommand.cs (endpoint + command pattern):
- Read these files to replicate the endpoint pattern with route params, request body, Mediator send, and response mapping.

Kafka topic name per D-11 (confirmed 2026-03-21): The auto-derived topic from `KebabCaseEndpointNameFormatter.SanitizeName("AttendanceRecordedIntegrationEvent")` is `attendance-recorded-integration-event`. This is the resolved and final topic name. Phase 5 consumer MUST subscribe to `attendance-recorded-integration-event`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain event, integration event, EventMapper, domain event handler, and DI wiring</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EventServices/Events/AttendanceRecordedEvent.cs,
    src/Services/Scheduling/Edvantix.Scheduling/IntegrationEvents/AttendanceRecordedIntegrationEvent.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EventServices/EventMapper.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/EventHandlers/AttendanceRecordedDomainEventHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/AttendanceRecord.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj,
    tests/Edvantix.Scheduling.UnitTests/Infrastructure/EventMapperTests.cs
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EventServices/Events/UserRoleAssignedEvent.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EventServices/EventMapper.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/EventHandlers/UserPermissionsDomainEventHandler.cs,
    src/Services/Organizations/Edvantix.Organizations/IntegrationEvents/UserPermissionsInvalidatedIntegrationEvent.cs,
    src/Services/Organizations/Edvantix.Organizations/Extensions/Extensions.cs,
    src/BuildingBlocks/Edvantix.Chassis/EventBus/IntegrationEvent.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/SeedWork/DomainEvent.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/AttendanceRecord.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj,
    tests/Edvantix.Organizations.UnitTests/Infrastructure/EventMapperTests.cs
  </read_first>
  <behavior>
    EventMapper tests:
    - GivenAttendanceRecordedEvent_WhenMapping_ThenAllFieldsAreMapped (StudentId, LessonSlotId, SchoolId, Status, MarkedAt as Timestamp, CorrelationId)
    - GivenAttendanceRecordedEvent_WhenMapping_ThenStatusIsString (not int)
    - GivenAttendanceRecordedEvent_WhenMapping_ThenCorrelationIdIsPreserved
    - GivenUnknownEvent_WhenMapping_ThenThrowsArgumentOutOfRangeException
  </behavior>
  <action>
    1. Create `Infrastructure/EventServices/Events/AttendanceRecordedEvent.cs`:
       - `public sealed class AttendanceRecordedEvent(Guid correlationId, Guid studentId, Guid lessonSlotId, Guid schoolId, string status, DateTimeOffset markedAt) : DomainEvent`
       - Properties: `CorrelationId`, `StudentId`, `LessonSlotId`, `SchoolId`, `Status` (string), `MarkedAt` (DateTimeOffset) — all with `{ get; } = paramName`
       - CRITICAL: `Status` is string, not enum — per D-12. The aggregate passes `status.ToString()` when creating this event.
       - Place in `Infrastructure/EventServices/Events/` folder (NOT Domain folder) to match Organizations pattern and pass arch tests that check domain events have no infrastructure imports.

    2. Create `IntegrationEvents/AttendanceRecordedIntegrationEvent.cs`:
       - Namespace: `Edvantix.Contracts` (per D-12, same as UserPermissionsInvalidatedIntegrationEvent)
       - `public sealed record AttendanceRecordedIntegrationEvent(Guid StudentId, Guid LessonSlotId, Guid SchoolId, string Status, DateTimeOffset Timestamp, Guid CorrelationId) : IntegrationEvent;`
       - CRITICAL: The file lives in the Scheduling project but uses `namespace Edvantix.Contracts` — this is the established pattern. The file MUST be in the Scheduling assembly (not a shared Contracts project) so `DiscoverMessageTypes` finds it during `RegisterKafkaProducers` assembly scan.
       - The Kafka topic name is `attendance-recorded-integration-event`, auto-derived by KebabCaseEndpointNameFormatter per D-11. This is the final, confirmed topic name. Phase 5 consumer MUST subscribe to `attendance-recorded-integration-event`.

    3. Create `Infrastructure/EventServices/EventMapper.cs`:
       - `internal sealed class EventMapper : IEventMapper` (same visibility as Organizations)
       - Single switch expression mapping `AttendanceRecordedEvent e => new AttendanceRecordedIntegrationEvent(e.StudentId, e.LessonSlotId, e.SchoolId, e.Status, e.MarkedAt, e.CorrelationId)`
       - Default case: `_ => throw new ArgumentOutOfRangeException(nameof(@event), @event, null)`
       - Note: `e.Status` is already a string (converted in the aggregate when creating the domain event), and `e.MarkedAt` maps to the `Timestamp` parameter in the integration event.

    4. Create `Domain/EventHandlers/AttendanceRecordedDomainEventHandler.cs`:
       - `public sealed class AttendanceRecordedDomainEventHandler(IEventDispatcher dispatcher) : INotificationHandler<AttendanceRecordedEvent>`
       - Single method: `public async ValueTask Handle(AttendanceRecordedEvent notification, CancellationToken cancellationToken) => await dispatcher.DispatchAsync(notification, cancellationToken);`
       - Import `Edvantix.Chassis.EventBus.Dispatcher` for `IEventDispatcher` and `Edvantix.Scheduling.Infrastructure.EventServices.Events` for the event type.

    5. Update `AttendanceRecord.cs` — uncomment/add `RegisterDomainEvent` calls:
       - In constructor after setting all properties: `RegisterDomainEvent(new AttendanceRecordedEvent(CorrelationId, StudentId, LessonSlotId, SchoolId, Status.ToString(), MarkedAt));`
       - In `UpdateStatus` after setting properties: `RegisterDomainEvent(new AttendanceRecordedEvent(CorrelationId, StudentId, LessonSlotId, SchoolId, Status.ToString(), MarkedAt));`
       - Add using for `Edvantix.Scheduling.Infrastructure.EventServices.Events`
       - CRITICAL: `Status.ToString()` converts enum to string per D-12.

    6. Edit `Extensions/Extensions.cs` — add two DI registrations:
       - After `builder.AddPersistenceServices();` line (approximately line 88), add:
         ```csharp
         services.AddScoped<IEventMapper, EventMapper>();
         services.AddEventDispatcher();
         ```
       - Add usings: `using Edvantix.Chassis.EventBus.Dispatcher;` and `using Edvantix.Scheduling.Infrastructure.EventServices;`
       - CRITICAL: Without these two lines, EventDispatcher throws NullReferenceException — domain events fire but never reach the outbox.

    7. Add `InternalsVisibleTo` to `Edvantix.Scheduling.csproj` for test access to `EventMapper` (internal class):
       - Add `<InternalsVisibleTo Include="Edvantix.Scheduling.UnitTests" />` inside an `<ItemGroup>`, following the Organizations pattern.

    8. Create `tests/Edvantix.Scheduling.UnitTests/Infrastructure/EventMapperTests.cs`:
       - Follow the Organizations EventMapperTests pattern
       - Test 1: `GivenAttendanceRecordedEvent_WhenMapping_ThenAllFieldsAreMapped` — create event with known values, map, assert all 6 fields match
       - Test 2: `GivenAttendanceRecordedEvent_WhenMapping_ThenStatusIsString` — assert `result.Status` is `"Present"` not `"0"`
       - Test 3: `GivenAttendanceRecordedEvent_WhenMapping_ThenCorrelationIdIsPreserved` — create event with specific CorrelationId, assert it matches after mapping
       - Test 4: `GivenUnknownDomainEvent_WhenMapping_ThenThrowsArgumentOutOfRangeException` — create a mock/stub DomainEvent subclass, assert throws
       - Use TUnit `[Test]` with `DisplayName`, Shouldly assertions
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling && dotnet test tests/Edvantix.Scheduling.UnitTests --filter "EventMapper" -x && dotnet test tests/Edvantix.ArchTests -x</automated>
  </verify>
  <acceptance_criteria>
    - `AttendanceRecordedEvent.cs` contains `sealed class AttendanceRecordedEvent` and `extends DomainEvent`
    - `AttendanceRecordedEvent.cs` contains `public string Status { get; }`
    - `AttendanceRecordedIntegrationEvent.cs` contains `namespace Edvantix.Contracts`
    - `AttendanceRecordedIntegrationEvent.cs` contains `sealed record AttendanceRecordedIntegrationEvent` and `IntegrationEvent`
    - `AttendanceRecordedIntegrationEvent.cs` contains `string Status`
    - `EventMapper.cs` contains `AttendanceRecordedEvent e => new AttendanceRecordedIntegrationEvent`
    - `EventMapper.cs` contains `internal sealed class EventMapper : IEventMapper`
    - `AttendanceRecordedDomainEventHandler.cs` contains `INotificationHandler<AttendanceRecordedEvent>`
    - `AttendanceRecordedDomainEventHandler.cs` contains `dispatcher.DispatchAsync`
    - `AttendanceRecord.cs` contains `RegisterDomainEvent(new AttendanceRecordedEvent(`
    - `AttendanceRecord.cs` contains `Status.ToString()`
    - `Extensions/Extensions.cs` contains `AddScoped<IEventMapper, EventMapper>()`
    - `Extensions/Extensions.cs` contains `AddEventDispatcher()`
    - `Edvantix.Scheduling.csproj` contains `InternalsVisibleTo` referencing `Edvantix.Scheduling.UnitTests`
    - `EventMapperTests.cs` contains at least 3 `[Test]` attributes
    - `dotnet test tests/Edvantix.Scheduling.UnitTests --filter "EventMapper" -x` exits 0
    - `dotnet test tests/Edvantix.ArchTests -x` exits 0
  </acceptance_criteria>
  <done>Domain event pipeline fully wired: AttendanceRecord raises domain event, handler dispatches to EventMapper, integration event produced to outbox. All EventMapper tests and arch tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MarkAttendance command (PUT endpoint with upsert) and handler tests</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommand.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommandHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceCommandValidator.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/Attendance/MarkAttendance/MarkAttendanceEndpoint.cs,
    tests/Edvantix.Scheduling.UnitTests/Features/MarkAttendanceCommandHandlerTests.cs
  </files>
  <read_first>
    src/Services/Scheduling/Edvantix.Scheduling/Features/LessonSlots/CreateLessonSlot/CreateLessonSlotCommand.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/LessonSlots/CreateLessonSlot/CreateLessonSlotCommandHandler.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/LessonSlots/CreateLessonSlot/CreateLessonSlotCommandValidator.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Features/LessonSlots/CreateLessonSlot/CreateLessonSlotEndpoint.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/AttendanceRecord.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/AttendanceAggregate/IAttendanceRecordRepository.cs,
    tests/Edvantix.Scheduling.UnitTests/Features/MarkAttendanceCommandHandlerTests.cs
  </read_first>
  <behavior>
    MarkAttendanceCommandHandler tests (un-skip the stubs from Plan 01):
    - GivenNoExistingRecord_WhenMarkingAttendance_ThenNewRecordIsAdded: repository.FindBySlotAndStudentAsync returns null, handler creates new AttendanceRecord, calls repository.Add, calls unitOfWork.SaveEntitiesAsync
    - GivenExistingRecord_WhenMarkingAttendance_ThenStatusIsUpdatedNotReAdded: repository.FindBySlotAndStudentAsync returns existing record, handler calls UpdateStatus on it (NOT repository.Add), calls unitOfWork.SaveEntitiesAsync
  </behavior>
  <action>
    1. Create `MarkAttendanceCommand.cs`:
       - `public sealed record MarkAttendanceCommand(Guid SlotId, Guid StudentId, AttendanceStatus Status) : ICommand<Unit>;`
       - Use `Mediator.Abstractions` `ICommand<Unit>` pattern (void return, per Phase 01-03 decision).

    2. Create `MarkAttendanceCommandHandler.cs`:
       - Primary constructor: `(IAttendanceRecordRepository repository, ITenantContext tenantContext)`
       - `IUnitOfWork UnitOfWork => repository.UnitOfWork;` (access SaveEntitiesAsync)
       - CRITICAL upsert pattern (per research — DO NOT use ExecuteUpdate):
         ```csharp
         var existing = await repository.FindBySlotAndStudentAsync(command.SlotId, command.StudentId, ct);
         if (existing is null)
         {
             var record = new AttendanceRecord(tenantContext.SchoolId, command.SlotId, command.StudentId, command.Status);
             repository.Add(record);
         }
         else
         {
             existing.UpdateStatus(command.Status);
         }
         await repository.UnitOfWork.SaveEntitiesAsync(ct);
         return Unit.Value;
         ```
       - The upsert uses EF change tracking so `EventDispatchInterceptor` captures domain events after `SaveChanges`. Using `ExecuteUpdate` would bypass change tracking and the domain event would NEVER reach the outbox.

    3. Create `MarkAttendanceCommandValidator.cs`:
       - Validate `SlotId` is not empty (default Guid)
       - Validate `StudentId` is not empty (default Guid)
       - Validate `Status` is a defined enum value: `RuleFor(x => x.Status).IsInEnum()`

    4. Create `MarkAttendanceEndpoint.cs`:
       - `PUT /v1/slots/{slotId}/attendance/{studentId}` per D-06
       - Request body record: `public sealed record MarkAttendanceRequest(AttendanceStatus Status);`
       - Maps route params + body to `MarkAttendanceCommand(slotId, studentId, request.Status)`
       - Returns 200 OK regardless of create vs update per D-06
       - Authorization: `.RequireAuthorization(SchedulingPermissions.MarkAttendance)` per D-08
       - Follow the existing endpoint pattern from CreateLessonSlotEndpoint.cs

    5. Update `MarkAttendanceCommandHandlerTests.cs`:
       - Remove `[Skip]` attributes from the stub tests created in Plan 01
       - Implement both tests using Moq:
         - Mock `IAttendanceRecordRepository` and `ITenantContext`
         - Test 1 (new record): `FindBySlotAndStudentAsync` returns null, verify `repository.Add` called once, verify `UnitOfWork.SaveEntitiesAsync` called once
         - Test 2 (existing record): `FindBySlotAndStudentAsync` returns an existing `AttendanceRecord`, verify `repository.Add` NOT called, verify `SaveEntitiesAsync` called once. Assert the record's Status changed.
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling && dotnet test tests/Edvantix.Scheduling.UnitTests -x && dotnet test tests/Edvantix.ArchTests -x</automated>
  </verify>
  <acceptance_criteria>
    - `MarkAttendanceCommand.cs` contains `ICommand<Unit>`
    - `MarkAttendanceCommandHandler.cs` contains `FindBySlotAndStudentAsync`
    - `MarkAttendanceCommandHandler.cs` contains `repository.Add(record)` inside the `if (existing is null)` branch
    - `MarkAttendanceCommandHandler.cs` contains `existing.UpdateStatus(command.Status)` inside the else branch
    - `MarkAttendanceCommandHandler.cs` contains `SaveEntitiesAsync`
    - `MarkAttendanceCommandHandler.cs` does NOT contain `ExecuteUpdate` or `ExecuteUpdateAsync`
    - `MarkAttendanceEndpoint.cs` contains `PUT` and `/slots/{slotId}/attendance/{studentId}`
    - `MarkAttendanceEndpoint.cs` contains `RequireAuthorization` referencing `MarkAttendance`
    - `MarkAttendanceCommandHandlerTests.cs` does NOT contain `Skip`
    - `MarkAttendanceCommandHandlerTests.cs` contains `GivenNoExistingRecord_WhenMarkingAttendance_ThenNewRecordIsAdded`
    - `MarkAttendanceCommandHandlerTests.cs` contains `GivenExistingRecord_WhenMarkingAttendance_ThenStatusIsUpdatedNotReAdded`
    - `dotnet test tests/Edvantix.Scheduling.UnitTests -x` exits 0
    - `dotnet test tests/Edvantix.ArchTests -x` exits 0
  </acceptance_criteria>
  <done>PUT endpoint works with upsert logic. Handler tests pass proving upsert is correct (new record = Add, existing = UpdateStatus). Full test suite green.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling` compiles without errors
2. `dotnet test tests/Edvantix.Scheduling.UnitTests -x` all tests pass (aggregate, handler, EventMapper)
3. `dotnet test tests/Edvantix.ArchTests -x` all arch tests pass
4. `just test` full solution test suite green
5. Verify domain event pipeline integrity: AttendanceRecord.cs contains RegisterDomainEvent calls in both constructor and UpdateStatus, EventMapper maps to integration event, Extensions.cs has AddScoped<IEventMapper, EventMapper>() and AddEventDispatcher()
</verification>

<success_criteria>
- PUT /v1/slots/{slotId}/attendance/{studentId} endpoint exists with MarkAttendance authorization
- Upsert handler uses FindBySlotAndStudentAsync + Add/UpdateStatus (NOT ExecuteUpdate)
- AttendanceRecordedEvent raised on every create and update
- EventMapper correctly maps all 6 fields including Status as string and CorrelationId
- IEventMapper and AddEventDispatcher registered in DI
- Integration event will reach outbox via existing MassTransit pipeline
- All unit tests and arch tests pass
- Kafka topic is `attendance-recorded-integration-event` per D-11
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduling-attendance-outbox/04-02-SUMMARY.md`
</output>
