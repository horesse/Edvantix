name: Frontend Shared CI Pipeline
run-name: Frontend Shared CI

on:
  workflow_call:
    inputs:
      retention-days:
        description: "Number of days to retain artifacts"
        type: number
        default: 7
    secrets:
      TURBO_TOKEN:
        description: "Turborepo remote cache token"
        required: false

permissions: {}

jobs:
  lint-build:
    name: Lint, Build
    timeout-minutes: 15
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write

    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
      TURBO_SCM_BASE: ${{ github.event.pull_request.base.sha || github.event.before || 'HEAD~1' }}
      TURBO_SCM_HEAD: ${{ github.sha }}

    defaults:
      run:
        working-directory: src/Clients

    steps:
      - name: Check out code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # fetch-depth: 2 is needed for Turbo's change detection (compares HEAD~1)
          fetch-depth: 2

      - name: Setup Frontend
        uses: ./.github/actions/frontend-setup

      - name: Lint
        run: pnpm turbo run lint --affected

      - name: Build
        run: pnpm turbo run build --affected