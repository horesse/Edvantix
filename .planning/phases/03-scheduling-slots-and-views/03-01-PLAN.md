---
phase: 03-scheduling-slots-and-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs
  - src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs
  - src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj
  - src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Program.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContextFactory.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Configurations/SchedulingAppSettings.cs
  - src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/ILessonSlotRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupMembershipConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/LessonSlotConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/GroupRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Grpc/Extensions.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/IPersonaProfileService.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/PersonaProfileService.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Grpc/Protos/persona/v1/profile.proto
  - src/Aspire/Edvantix.AppHost/AppHost.cs
  - src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json
  - tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj
  - tests/Edvantix.ArchTests/Abstractions/BaseTest.cs
  - tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs
  - tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Properties/launchSettings.json
  - src/Services/Scheduling/Edvantix.Scheduling/appsettings.json
  - src/Services/Scheduling/Edvantix.Scheduling/appsettings.Development.json
autonomous: true
requirements: [SCH-10, SCH-01, SCH-06]

must_haves:
  truths:
    - "Scheduling service compiles and is visible as a resource in Aspire dashboard"
    - "Group and LessonSlot domain aggregates exist with correct ITenanted + ISoftDelete interfaces"
    - "GroupMembership entity carries SchoolId and has HasQueryFilter in SchedulingDbContext"
    - "Architecture test for SchedulingDbContext tenant isolation passes"
    - "All date/time properties use DateTimeOffset (SCH-10)"
    - "Scheduling permissions are seeded to Organizations via HTTP on startup"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj"
      provides: "Scheduling service project"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs"
      provides: "Group aggregate root"
      contains: "IAggregateRoot, ISoftDelete, ITenanted"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs"
      provides: "LessonSlot aggregate root"
      contains: "IAggregateRoot, ITenanted"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs"
      provides: "DbContext with HasQueryFilter for Group, LessonSlot, GroupMembership"
      contains: "HasQueryFilter"
    - path: "tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs"
      provides: "Arch test for Scheduling tenant isolation"
    - path: "src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs"
      provides: "Permission string constants"
      contains: "scheduling.create-group"
  key_links:
    - from: "src/Aspire/Edvantix.AppHost/AppHost.cs"
      to: "src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj"
      via: "AddProject<Edvantix_Scheduling>"
      pattern: "AddProject.*Scheduling"
    - from: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs"
      to: "Group, LessonSlot, GroupMembership"
      via: "HasQueryFilter in OnModelCreating"
      pattern: "HasQueryFilter"
    - from: "tests/Edvantix.ArchTests/Abstractions/BaseTest.cs"
      to: "src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs"
      via: "Assembly reference"
      pattern: "ISchedulingApiMarker"
---

<objective>
Bootstrap the Scheduling service from scratch and establish the Group + LessonSlot domain model with EF Core configuration, tenant isolation, and architecture tests.

Purpose: Create the entire Scheduling service skeleton (project, Aspire wiring, Keycloak client, domain model, DbContext, permission seeder, arch tests) so that Plans 02-04 can build features on top of a compiling, Aspire-registered service.
Output: Compiling Scheduling service visible in Aspire dashboard; Group/LessonSlot/GroupMembership domain model with EF configuration; passing arch tests for tenant isolation; permission constants in Edvantix.Constants.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scheduling-slots-and-views/03-CONTEXT.md
@.planning/phases/03-scheduling-slots-and-views/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Scheduling service project, constants, Aspire wiring, Keycloak client, and permission seeder</name>
  <files>
    src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs,
    src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj,
    src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Program.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Extensions/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContextFactory.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Configurations/SchedulingAppSettings.cs,
    src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Extensions.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/IPersonaProfileService.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Services/PersonaProfileService.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Grpc/Protos/persona/v1/profile.proto,
    src/Services/Scheduling/Edvantix.Scheduling/Properties/launchSettings.json,
    src/Services/Scheduling/Edvantix.Scheduling/appsettings.json,
    src/Services/Scheduling/Edvantix.Scheduling/appsettings.Development.json,
    src/Aspire/Edvantix.AppHost/AppHost.cs,
    src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Edvantix.Organizations.csproj,
    src/Services/Organizations/Edvantix.Organizations/Program.cs,
    src/Services/Organizations/Edvantix.Organizations/Extensions/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/OrganizationsDbContext.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/OrganizationsDbContextFactory.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Seeding/PermissionSeeder.cs,
    src/Services/Organizations/Edvantix.Organizations/Configurations/OrganizationsAppSettings.cs,
    src/Services/Organizations/Edvantix.Organizations/GlobalUsings.cs,
    src/Services/Organizations/Edvantix.Organizations/IOrganizationsApiMarker.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Extensions.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Services/IPersonaProfileService.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Services/PersonaProfileService.cs,
    src/Services/Organizations/Edvantix.Organizations/Grpc/Protos/persona/v1/profile.proto,
    src/Services/Organizations/Edvantix.Organizations/Properties/launchSettings.json,
    src/Services/Organizations/Edvantix.Organizations/appsettings.json,
    src/Services/Organizations/Edvantix.Organizations/appsettings.Development.json,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs,
    src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs,
    src/BuildingBlocks/Edvantix.Constants/Permissions/OrganizationsPermissions.cs,
    src/Aspire/Edvantix.AppHost/AppHost.cs,
    src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json,
    src/Aspire/Edvantix.AppHost/Extensions/Security/KeycloakExtensions.cs
  </read_first>
  <action>
**Constants (per D-11, D-12):**

1. Add to `Services.cs`:
```csharp
public static readonly string Scheduling = nameof(Scheduling).ToLowerInvariant();
```

2. Add to `Components.Database`:
```csharp
public static readonly string Scheduling = $"{nameof(Scheduling).ToLowerInvariant()}{Suffix}";
```

3. Create `SchedulingPermissions.cs` in `Edvantix.Constants/Permissions/` following `OrganizationsPermissions.cs` pattern. Permission strings (per D-12):
- `scheduling.create-group`
- `scheduling.update-group`
- `scheduling.delete-group`
- `scheduling.create-lesson-slot`
- `scheduling.edit-lesson-slot`
- `scheduling.delete-lesson-slot`
- `scheduling.view-schedule`
- `scheduling.manage-group-membership`

Include `public static IReadOnlyList<string> All => [...]` collecting all 8 constants.

**Scheduling service project:**

4. Create `Edvantix.Scheduling.csproj` mirroring `Edvantix.Organizations.csproj` exactly:
- Same `Sdk="Microsoft.NET.Sdk.Web"`, same `SuppressCheckGrpcNetClientFactoryVersion`, same `InternalsVisibleTo` for `DynamicProxyGenAssembly2` and `$(AssemblyName).UnitTests`
- Same PackageReferences: `Grpc.Tools`, `Grpc.AspNetCore.Server.ClientFactory`, `MassTransit.EntityFrameworkCore`, `Mediator.SourceGenerator`, `Microsoft.EntityFrameworkCore.Tools`
- ProjectReference to `Edvantix.ServiceDefaults`
- Protobuf entry: `<Protobuf Include="Grpc\Protos\persona\v1\profile.proto" GrpcServices="Client" />`
- NO permissions.proto entry (Scheduling uses Chassis-generated types, same as Organizations)

5. Create `ISchedulingApiMarker.cs`: empty marker interface `public interface ISchedulingApiMarker;` in namespace `Edvantix.Scheduling`.

6. Create `Program.cs` following Organizations pattern exactly:
- `builder.WebHost.ConfigureKestrel(o => o.AddServerHeader = false);`
- `builder.AddServiceDefaults(); builder.AddApplicationServices();`
- Middleware pipeline: `UseExceptionHandler`, `UseStatusCodePages`, `UseDefaultCors`, `UseKeycloakTokenIntrospection`, `UseRateLimiter`, `UseTenantContext` (BEFORE UseAuthorization)
- `app.NewApiVersionSet()` with `ApiVersions.V1`
- `app.MapEndpoints(apiVersionSet);`
- `app.MapDefaultEndpoints(); app.UseDefaultOpenApi(); app.UseAuthorization(); app.Run();`
- NO `MapGrpcService` (Scheduling is a gRPC CLIENT, not server)

7. Create `Extensions/Extensions.cs` (`AddApplicationServices`) following Organizations pattern:
- `AddDefaultCors`, `AddDefaultAuthentication().WithKeycloakClaimsTransformation()`
- Authorization: default policy with `RequireAuthenticatedUser().RequireScope($"{Services.Scheduling}_{Authorization.Actions.Read}")`
- `AddPermissionAuthorization(...)` pointing at `Services.Organizations` URL (per Phase 02-03 pattern) with `HttpUtilities.AsUrlBuilder().WithScheme(builder.GetScheme()).WithHost(Services.Organizations).Build()`
- Add authorization policies for each scheduling permission using `.AddPolicy(SchedulingPermissions.CreateGroup, p => p.RequirePermission(SchedulingPermissions.CreateGroup))` and repeat for all 8 permissions
- `AddTenantContext`, exception handlers, `AddMediator`, `AddAppSettings<SchedulingAppSettings>`, `AddRateLimiting`
- `AddPersistenceServices`, validators from `ISchedulingApiMarker`, `ClaimsPrincipal` transient, activity/metrics
- `AddVersioning`, `AddEndpoints(typeof(ISchedulingApiMarker))`, `AddDefaultOpenApi` with `OpenApiInfoDefinitionsTransformer<SchedulingAppSettings>`
- `AddEventBus` with `AddEntityFrameworkOutbox<SchedulingDbContext>` (wire Kafka now for Phase 4)
- `AddKeycloakTokenIntrospection`, `AddGrpcServices` (Persona client)
- `AddHostedService<PermissionSeeder>()`
- Do NOT include `AddMapper`/`AddEventDispatcher`/`AddScoped<IEventMapper>` yet (no domain events in Phase 3)

8. Create `Infrastructure/Extensions.cs` (`AddPersistenceServices`) mirroring Organizations:
```csharp
builder.AddAzurePostgresDbContext<SchedulingDbContext>(
    Components.Database.Scheduling,
    _ => { services.AddMigration<SchedulingDbContext>(); services.AddRepositories(typeof(ISchedulingApiMarker)); }
);
```

9. Create `SchedulingDbContext` mirroring `OrganizationsDbContext`:
- Primary constructor: `(DbContextOptions<SchedulingDbContext> options, ITenantContext tenantContext)`
- Implements `IUnitOfWork`
- DbSets: `Groups` (`DbSet<Group>`), `LessonSlots` (`DbSet<LessonSlot>`), `GroupMemberships` (`DbSet<GroupMembership>`)
- `OnModelCreating`: call `base.OnModelCreating`, `AddInboxStateEntity`, `AddOutboxMessageEntity`, `AddOutboxStateEntity`, `ApplyConfigurationsFromAssembly`
- HasQueryFilter for Group: `g => g.SchoolId == tenantContext.SchoolId && !g.IsDeleted` (per D-02: combined tenant + soft-delete)
- HasQueryFilter for LessonSlot: `s => s.SchoolId == tenantContext.SchoolId` (tenant only, no soft delete)
- HasQueryFilter for GroupMembership: `m => m.SchoolId == tenantContext.SchoolId` (denormalized SchoolId per Research recommendation)
- `SaveEntitiesAsync` identical to Organizations

10. Create `SchedulingDbContextFactory` mirroring `OrganizationsDbContextFactory` for EF Core migrations tooling.

11. Create `PermissionSeeder.cs` as `IHostedService` that uses `IHttpClientFactory` (NOT `IPermissionRepository` which is Organizations-only):
- Inject `IHttpClientFactory` and `ILogger<PermissionSeeder>`
- `StartAsync`: create client via `httpClientFactory.CreateClient("organizations")`, POST to `/v1/permissions/register` with body `{ Names = SchedulingPermissions.All }`, call `response.EnsureSuccessStatusCode()`
- Register the named HttpClient in `Extensions.cs`: `services.AddHttpClient("organizations", c => c.BaseAddress = new Uri($"http://{Services.Organizations}"))`
- Add retry with Polly for startup ordering resilience

12. Create `SchedulingAppSettings.cs` mirroring `OrganizationsAppSettings.cs`.

13. Create `GlobalUsings.cs` with namespace imports for Group and LessonSlot aggregates (matching Organizations pattern of adding aggregate namespaces to GlobalUsings).

14. Copy `Grpc/Protos/persona/v1/profile.proto` from Organizations. Create `Grpc/Extensions.cs`, `Grpc/Services/IPersonaProfileService.cs`, `Grpc/Services/PersonaProfileService.cs` mirroring Organizations gRPC client wiring.

15. Create `Properties/launchSettings.json`, `appsettings.json`, `appsettings.Development.json` mirroring Organizations (different ports).

**Aspire wiring (per D-13):**

16. In `AppHost.cs`, add after `organizationsDb` line:
```csharp
var schedulingDb = postgres.AddDatabase(Components.Database.Scheduling);
```
Add after `organizationsApi` block:
```csharp
var schedulingApi = builder
    .AddProject<Edvantix_Scheduling>(Services.Scheduling)
    .WithReference(schedulingDb)
    .WaitFor(schedulingDb)
    .WithKeycloak(keycloak)
    .WaitFor(keycloak)
    .WithReference(personaApi)
    .WaitFor(personaApi)
    .WithReference(organizationsApi)
    .WaitFor(organizationsApi)
    .WithReference(queue)
    .WaitFor(queue)
    .WithContainerRegistry(registry)
    .WithFriendlyUrls();
```
Add to gateway: `.WithService(schedulingApi, true)` in the `AddApiGatewayProxy()` chain.
Add to Scalar: `.WithOpenAPI(schedulingApi)` in the `AddScalar` block.

**Keycloak (per D-14):**

17. In `EdvantixRealms.json`, add a new client entry for `${CLIENT_SCHEDULING_ID}` following the exact structure of the `${CLIENT_PERSONA_ID}` client entry (copy the Persona client block, replace all `PERSONA` with `SCHEDULING`, generate new UUIDs for `id` fields). Include `service_account` in defaultClientScopes. Also add the corresponding `service-account-${CLIENT_SCHEDULING_ID}` user entry, `protocolMappers` section, and `clientScopeMappings` entry following Persona's exact pattern.
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` exists and contains `Sdk="Microsoft.NET.Sdk.Web"`
    - `src/BuildingBlocks/Edvantix.Constants/Aspire/Services.cs` contains `public static readonly string Scheduling`
    - `src/BuildingBlocks/Edvantix.Constants/Aspire/Components.cs` contains `public static readonly string Scheduling`
    - `src/BuildingBlocks/Edvantix.Constants/Permissions/SchedulingPermissions.cs` contains `scheduling.create-group` and `scheduling.view-schedule` and `IReadOnlyList<string> All`
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs` contains `HasQueryFilter` (at least 3 occurrences for Group, LessonSlot, GroupMembership)
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs` contains `tenantContext.SchoolId` and `!g.IsDeleted`
    - `src/Services/Scheduling/Edvantix.Scheduling/Program.cs` contains `UseTenantContext` appearing before `UseAuthorization`
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Seeding/PermissionSeeder.cs` contains `IHttpClientFactory` and `/v1/permissions/register`
    - `src/Aspire/Edvantix.AppHost/AppHost.cs` contains `Components.Database.Scheduling` and `Services.Scheduling` and `schedulingApi`
    - `src/Aspire/Edvantix.AppHost/Container/keycloak/realms/EdvantixRealms.json` contains `CLIENT_SCHEDULING_ID`
    - `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` exits 0
  </acceptance_criteria>
  <done>Scheduling service project compiles. Constants for service name, database, and 8 permissions exist. Aspire AppHost registers schedulingDb + schedulingApi with all dependencies. Keycloak realm includes Scheduling client. PermissionSeeder calls Organizations HTTP endpoint. SchedulingDbContext has HasQueryFilter for all 3 tenanted entities.</done>
</task>

<task type="auto">
  <name>Task 2: Create Group and LessonSlot domain aggregates, EF configurations, repositories, and architecture tests</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/ILessonSlotRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupMembershipConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/LessonSlotConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/GroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs,
    tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj,
    tests/Edvantix.ArchTests/Abstractions/BaseTest.cs,
    tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs,
    tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/Role.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/RolePermission.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/IRoleRepository.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/UserRoleAssignmentAggregate/UserRoleAssignment.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/RoleConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/RolePermissionConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/UserRoleAssignmentConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Repositories/RoleRepository.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/Entity.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/IAggregateRoot.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/ISoftDelete.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/Abstractions/ITenanted.cs,
    tests/Edvantix.ArchTests/Abstractions/BaseTest.cs,
    tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs,
    tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj,
    tests/Edvantix.ArchTests/Domain/TenantIsolationTests.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs
  </read_first>
  <action>
**Domain aggregates (per D-01, D-02, D-03, SCH-10):**

1. Create `Group.cs`:
```csharp
public sealed class Group : Entity, IAggregateRoot, ISoftDelete, ITenanted
{
    private readonly List<GroupMembership> _members = [];

    public string Name { get; private set; } = string.Empty;
    public Guid SchoolId { get; private set; }
    public int MaxCapacity { get; private set; }
    public string Color { get; private set; } = string.Empty;
    public bool IsDeleted { get; set; }

    public IReadOnlyCollection<GroupMembership> Members => _members.AsReadOnly();

    private Group() { }

    public Group(string name, Guid schoolId, int maxCapacity, string color)
    {
        Guard.Against.NullOrWhiteSpace(name, nameof(name));
        Guard.Against.Default(schoolId, nameof(schoolId));
        Name = name.Trim();
        SchoolId = schoolId;
        MaxCapacity = maxCapacity;
        Color = color;
    }

    public void Update(string name, int maxCapacity, string color)
    {
        Guard.Against.NullOrWhiteSpace(name, nameof(name));
        Name = name.Trim();
        MaxCapacity = maxCapacity;
        Color = color;
    }

    public void Delete() => IsDeleted = true;

    public void AddMember(Guid studentId, DateTimeOffset joinedAt)
    {
        Guard.Against.Default(studentId, nameof(studentId));
        if (_members.Any(m => m.StudentId == studentId)) return;
        _members.Add(new GroupMembership(Id, SchoolId, studentId, joinedAt));
    }

    public void RemoveMember(Guid studentId)
    {
        var existing = _members.FirstOrDefault(m => m.StudentId == studentId);
        if (existing is not null) _members.Remove(existing);
    }
}
```

2. Create `GroupMembership.cs` as an Entity (NOT IAggregateRoot) within Group aggregate:
```csharp
public sealed class GroupMembership : Entity, ITenanted
{
    public Guid GroupId { get; private set; }
    public Guid SchoolId { get; private set; }
    public Guid StudentId { get; private set; }
    public DateTimeOffset JoinedAt { get; private set; }

    private GroupMembership() { }

    public GroupMembership(Guid groupId, Guid schoolId, Guid studentId, DateTimeOffset joinedAt)
    {
        Guard.Against.Default(groupId, nameof(groupId));
        Guard.Against.Default(schoolId, nameof(schoolId));
        Guard.Against.Default(studentId, nameof(studentId));
        GroupId = groupId;
        SchoolId = schoolId;
        StudentId = studentId;
        JoinedAt = joinedAt;
    }
}
```
SchoolId is denormalized (not via FK navigation) per Research recommendation and UserRoleAssignment pattern.

3. Create `IGroupRepository.cs`:
```csharp
public interface IGroupRepository : IRepository<Group>
{
    Task<Group?> FindByIdAsync(Guid groupId, CancellationToken ct);
    Task<Group?> FindByIdWithMembersAsync(Guid groupId, CancellationToken ct);
    Task<List<Group>> GetBySchoolAsync(CancellationToken ct);
    void Add(Group group);
    void Remove(Group group);
}
```

4. Create `LessonSlot.cs`:
```csharp
public sealed class LessonSlot : Entity, IAggregateRoot, ITenanted
{
    public Guid SchoolId { get; private set; }
    public Guid GroupId { get; private set; }
    public Guid TeacherId { get; private set; }
    public DateTimeOffset StartTime { get; private set; }
    public DateTimeOffset EndTime { get; private set; }

    private LessonSlot() { }

    public LessonSlot(Guid schoolId, Guid groupId, Guid teacherId,
                      DateTimeOffset startTime, DateTimeOffset endTime)
    {
        Guard.Against.Default(schoolId, nameof(schoolId));
        Guard.Against.Default(groupId, nameof(groupId));
        Guard.Against.Default(teacherId, nameof(teacherId));
        if (endTime <= startTime) throw new ArgumentException("EndTime must be after StartTime");
        SchoolId = schoolId;
        GroupId = groupId;
        TeacherId = teacherId;
        StartTime = startTime;
        EndTime = endTime;
    }

    public void Reschedule(DateTimeOffset newStart, DateTimeOffset newEnd)
    {
        if (newEnd <= newStart) throw new ArgumentException("EndTime must be after StartTime");
        StartTime = newStart;
        EndTime = newEnd;
    }

    public void ChangeTeacher(Guid newTeacherId)
    {
        Guard.Against.Default(newTeacherId, nameof(newTeacherId));
        TeacherId = newTeacherId;
    }
}
```

5. Create `ILessonSlotRepository.cs`:
```csharp
public interface ILessonSlotRepository : IRepository<LessonSlot>
{
    Task<LessonSlot?> FindByIdAsync(Guid slotId, CancellationToken ct);
    void Add(LessonSlot slot);
    void Remove(LessonSlot slot);
    /// <summary>
    /// Checks for teacher time conflicts GLOBALLY (across all schools) using IgnoreQueryFilters().
    /// Per D-04: a teacher cannot be double-booked across schools.
    /// Per D-05: overlap = slot.StartTime &lt; existing.EndTime AND slot.EndTime &gt; existing.StartTime.
    /// Per D-06: excludedSlotId is used when editing to exclude the current slot.
    /// </summary>
    Task<bool> HasConflictAsync(Guid teacherId, DateTimeOffset startTime, DateTimeOffset endTime, Guid? excludedSlotId, CancellationToken ct);
}
```

**EF Core configurations:**

6. Create `GroupConfiguration.cs`: table name `groups`, `Name` max length 200, `Color` max length 50, configure `_members` backing field via `builder.HasMany(g => g.Members).WithOne().HasForeignKey(m => m.GroupId)` with `PropertyAccessMode.Field` for the `_members` navigation (same pattern as Role._permissions).

7. Create `GroupMembershipConfiguration.cs`: table name `group_memberships`, unique index on `(GroupId, StudentId)` to prevent duplicate membership, `JoinedAt` with `.HasColumnType("timestamp with time zone")` (per SCH-10).

8. Create `LessonSlotConfiguration.cs`: table name `lesson_slots`, `StartTime` and `EndTime` with `.HasColumnType("timestamp with time zone")` (per SCH-10). Add composite index on `(TeacherId, StartTime, EndTime)` via `.HasIndex(s => new { s.TeacherId, s.StartTime, s.EndTime })` for conflict detection query performance.

**Repositories:**

9. Create `GroupRepository.cs` implementing `IGroupRepository`. `FindByIdWithMembersAsync` must `.Include(g => g.Members)`. Follow `RoleRepository` pattern.

10. Create `LessonSlotRepository.cs` implementing `ILessonSlotRepository`. The `HasConflictAsync` method MUST use `_context.LessonSlots.IgnoreQueryFilters()` for global conflict check:
```csharp
public async Task<bool> HasConflictAsync(
    Guid teacherId, DateTimeOffset startTime, DateTimeOffset endTime,
    Guid? excludedSlotId, CancellationToken ct)
{
    return await _context.LessonSlots
        .IgnoreQueryFilters()
        .AnyAsync(s =>
            s.TeacherId == teacherId
            && s.StartTime < endTime
            && s.EndTime > startTime
            && (excludedSlotId == null || s.Id != excludedSlotId.Value),
            ct);
}
```

**Architecture tests:**

11. In `Edvantix.ArchTests.csproj`, add ProjectReference:
```xml
<ProjectReference Include="..\..\src\Services\Scheduling\Edvantix.Scheduling\Edvantix.Scheduling.csproj" />
```

12. In `BaseTest.cs`, add:
```csharp
using Edvantix.Scheduling;
// ... in the class body:
protected static readonly Assembly SchedulingAssembly = typeof(ISchedulingApiMarker).Assembly;
```

13. In `ArchUnitBaseTest.cs`:
- Add `SchedulingAssembly` to the `.LoadAssemblies(...)` call
- Add `SchedulingServiceTypes` object provider following the existing pattern (same as `OrganizationsServiceTypes` but with `SchedulingAssembly`)
- Add `nameof(Scheduling) => SchedulingServiceTypes` to the `GetServiceTypes` switch expression

14. Create `SchedulingTenantIsolationTests.cs` following `TenantIsolationTests.cs` pattern:
- Test method: `GivenTenantedEntities_WhenCheckingSchedulingDbContextModel_ThenAllShouldHaveQueryFilter`
- Construct `TenantContext`, resolve a GUID, build `SchedulingDbContext` with InMemory provider
- Find all entity types implementing `ITenanted`, assert each has `GetDeclaredQueryFilters().Any()`
- Verify at least 3 ITenanted entities exist (Group, LessonSlot, GroupMembership)
  </action>
  <verify>
    <automated>dotnet test tests/Edvantix.ArchTests --filter "SchedulingTenantIsolation" --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs` contains `IAggregateRoot, ISoftDelete, ITenanted` and `AddMember` and `RemoveMember`
    - `src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs` contains `ITenanted` and `DateTimeOffset JoinedAt`
    - `src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs` contains `IAggregateRoot, ITenanted` and `DateTimeOffset StartTime` and `DateTimeOffset EndTime`
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs` contains `IgnoreQueryFilters()`
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/LessonSlotConfiguration.cs` contains `timestamp with time zone` and `HasIndex`
    - `src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupMembershipConfiguration.cs` contains `timestamp with time zone`
    - `tests/Edvantix.ArchTests/Abstractions/BaseTest.cs` contains `ISchedulingApiMarker`
    - `tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs` contains `SchedulingAssembly` and `SchedulingServiceTypes`
    - `tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs` contains `SchedulingDbContext` and `GivenTenantedEntities_WhenCheckingSchedulingDbContextModel`
    - `dotnet test tests/Edvantix.ArchTests --filter "SchedulingTenantIsolation"` exits 0
  </acceptance_criteria>
  <done>Group aggregate with Name/SchoolId/MaxCapacity/Color/ISoftDelete and AddMember/RemoveMember methods exists. LessonSlot aggregate with SchoolId/GroupId/TeacherId/StartTime(DateTimeOffset)/EndTime(DateTimeOffset) and Reschedule/ChangeTeacher methods exists. GroupMembership entity with denormalized SchoolId exists. All EF configurations have correct column types for DateTimeOffset. LessonSlot has composite index on (TeacherId, StartTime, EndTime). GroupMembership has unique index on (GroupId, StudentId). LessonSlotRepository.HasConflictAsync uses IgnoreQueryFilters for global conflict check. Architecture tests pass confirming all 3 ITenanted entities have HasQueryFilter.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` succeeds
2. `dotnet test tests/Edvantix.ArchTests` passes (includes new SchedulingTenantIsolation tests)
3. `grep -r "DateTime\.Now\|DateTime\.UtcNow" src/Services/Scheduling/` returns no matches (SCH-10 compliance)
4. `grep -c "HasQueryFilter" src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs` returns 3
</verification>

<success_criteria>
- Scheduling service compiles as an independent project
- Aspire AppHost.cs registers schedulingDb + schedulingApi with Keycloak, Persona, Organizations, Kafka references
- Keycloak realm JSON includes Scheduling client entry
- All 8 scheduling permission constants defined
- PermissionSeeder uses HTTP to Organizations (not direct DB)
- Group, LessonSlot, GroupMembership domain model complete with correct interfaces
- All DateTimeOffset columns mapped to `timestamp with time zone`
- Architecture tests pass for Scheduling tenant isolation
- No DateTime.Now/UtcNow usage in Scheduling service code
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-slots-and-views/03-01-SUMMARY.md`
</output>
