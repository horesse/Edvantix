---
phase: 03-scheduling-slots-and-views
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/ILessonSlotRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupMembershipConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/LessonSlotConfiguration.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/GroupRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs
  - src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs
  - src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs
  - tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj
  - tests/Edvantix.ArchTests/Abstractions/BaseTest.cs
  - tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs
  - tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs
  - tests/Edvantix.Scheduling.UnitTests/Edvantix.Scheduling.UnitTests.csproj
  - tests/Edvantix.Scheduling.UnitTests/GlobalUsings.cs
  - tests/Edvantix.Scheduling.UnitTests/Domain/GroupAggregateTests.cs
  - tests/Edvantix.Scheduling.UnitTests/Domain/LessonSlotAggregateTests.cs
autonomous: true
requirements: [SCH-01, SCH-06, SCH-10]

must_haves:
  truths:
    - "Group and LessonSlot domain aggregates exist with correct ITenanted + ISoftDelete interfaces"
    - "GroupMembership entity carries SchoolId and has HasQueryFilter in SchedulingDbContext"
    - "Architecture test for SchedulingDbContext tenant isolation passes"
    - "All date/time properties use DateTimeOffset (SCH-10)"
    - "Unit tests for Group.AddMember, Group.RemoveMember, LessonSlot constructor, LessonSlot.Reschedule pass"
    - "LessonSlotRepository.HasConflictAsync uses IgnoreQueryFilters for global conflict check"
  artifacts:
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs"
      provides: "Group aggregate root"
      contains: "IAggregateRoot, ISoftDelete, ITenanted"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs"
      provides: "LessonSlot aggregate root"
      contains: "IAggregateRoot, ITenanted"
    - path: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs"
      provides: "DbContext with HasQueryFilter for Group, LessonSlot, GroupMembership"
      contains: "HasQueryFilter"
    - path: "tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs"
      provides: "Arch test for Scheduling tenant isolation"
    - path: "tests/Edvantix.Scheduling.UnitTests/Domain/GroupAggregateTests.cs"
      provides: "Unit tests for Group aggregate behavior"
    - path: "tests/Edvantix.Scheduling.UnitTests/Domain/LessonSlotAggregateTests.cs"
      provides: "Unit tests for LessonSlot aggregate behavior"
  key_links:
    - from: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs"
      to: "Group, LessonSlot, GroupMembership"
      via: "HasQueryFilter in OnModelCreating"
      pattern: "HasQueryFilter"
    - from: "tests/Edvantix.ArchTests/Abstractions/BaseTest.cs"
      to: "src/Services/Scheduling/Edvantix.Scheduling/ISchedulingApiMarker.cs"
      via: "Assembly reference"
      pattern: "ISchedulingApiMarker"
    - from: "src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs"
      to: "SchedulingDbContext.LessonSlots.IgnoreQueryFilters()"
      via: "Global conflict check bypassing tenant filter"
      pattern: "IgnoreQueryFilters"
---

<objective>
Create Group and LessonSlot domain aggregates, EF configurations, repositories, architecture tests, and unit test project with skeleton tests.

Purpose: Establish the domain model foundation that Plans 03-06 build features on top of. Create the unit test project that provides automated regression protection for aggregate behavior.
Output: Compiling domain model with passing arch tests and unit tests; HasQueryFilter for all tenanted entities; LessonSlotRepository with global conflict check.
</objective>

<execution_context>
@E:/projects/Edvantix/.claude/get-shit-done/workflows/execute-plan.md
@E:/projects/Edvantix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scheduling-slots-and-views/03-CONTEXT.md
@.planning/phases/03-scheduling-slots-and-views/03-RESEARCH.md
@.planning/phases/03-scheduling-slots-and-views/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Group and LessonSlot domain aggregates, EF configurations, repositories, and update SchedulingDbContext</name>
  <files>
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/Group.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/GroupMembership.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/GroupAggregate/IGroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/LessonSlot.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Domain/AggregatesModel/LessonSlotAggregate/ILessonSlotRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/GroupMembershipConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/EntityConfigurations/LessonSlotConfiguration.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/GroupRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/Repositories/LessonSlotRepository.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs,
    src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs
  </files>
  <read_first>
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/Role.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/RolePermission.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/RoleAggregate/IRoleRepository.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/AggregatesModel/UserRoleAssignmentAggregate/UserRoleAssignment.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/RoleConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/RolePermissionConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/EntityConfigurations/UserRoleAssignmentConfiguration.cs,
    src/Services/Organizations/Edvantix.Organizations/Infrastructure/Repositories/RoleRepository.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/Entity.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/IAggregateRoot.cs,
    src/BuildingBlocks/Edvantix.SharedKernel/Domain/ISoftDelete.cs,
    src/Services/Organizations/Edvantix.Organizations/Domain/Abstractions/ITenanted.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs,
    src/Services/Scheduling/Edvantix.Scheduling/GlobalUsings.cs
  </read_first>
  <behavior>
    - Group constructor with valid args sets Name, SchoolId, MaxCapacity, Color
    - Group constructor with null/whitespace name throws
    - Group constructor with default schoolId throws
    - Group.Update changes Name, MaxCapacity, Color
    - Group.Delete sets IsDeleted to true
    - Group.AddMember with new studentId adds member
    - Group.AddMember with existing studentId is no-op (idempotent)
    - Group.RemoveMember with existing studentId removes member
    - Group.RemoveMember with non-existing studentId is no-op
    - LessonSlot constructor with valid args sets all properties
    - LessonSlot constructor with endTime <= startTime throws ArgumentException
    - LessonSlot.Reschedule updates StartTime and EndTime
    - LessonSlot.Reschedule with endTime <= startTime throws
    - LessonSlot.ChangeTeacher updates TeacherId
    - LessonSlot.ChangeTeacher with default guid throws
  </behavior>
  <action>
**Domain aggregates (per D-01, D-02, D-03, SCH-10):**

1. Create `Group.cs`:
```csharp
public sealed class Group : Entity, IAggregateRoot, ISoftDelete, ITenanted
{
    private readonly List<GroupMembership> _members = [];

    public string Name { get; private set; } = string.Empty;
    public Guid SchoolId { get; private set; }
    public int MaxCapacity { get; private set; }
    public string Color { get; private set; } = string.Empty;
    public bool IsDeleted { get; set; }

    public IReadOnlyCollection<GroupMembership> Members => _members.AsReadOnly();

    private Group() { }

    public Group(string name, Guid schoolId, int maxCapacity, string color)
    {
        Guard.Against.NullOrWhiteSpace(name, nameof(name));
        Guard.Against.Default(schoolId, nameof(schoolId));
        Name = name.Trim();
        SchoolId = schoolId;
        MaxCapacity = maxCapacity;
        Color = color;
    }

    public void Update(string name, int maxCapacity, string color)
    {
        Guard.Against.NullOrWhiteSpace(name, nameof(name));
        Name = name.Trim();
        MaxCapacity = maxCapacity;
        Color = color;
    }

    public void Delete() => IsDeleted = true;

    public void AddMember(Guid studentId, DateTimeOffset joinedAt)
    {
        Guard.Against.Default(studentId, nameof(studentId));
        if (_members.Any(m => m.StudentId == studentId)) return;
        _members.Add(new GroupMembership(Id, SchoolId, studentId, joinedAt));
    }

    public void RemoveMember(Guid studentId)
    {
        var existing = _members.FirstOrDefault(m => m.StudentId == studentId);
        if (existing is not null) _members.Remove(existing);
    }
}
```

2. Create `GroupMembership.cs` as an Entity (NOT IAggregateRoot) within Group aggregate:
```csharp
public sealed class GroupMembership : Entity, ITenanted
{
    public Guid GroupId { get; private set; }
    public Guid SchoolId { get; private set; }
    public Guid StudentId { get; private set; }
    public DateTimeOffset JoinedAt { get; private set; }

    private GroupMembership() { }

    public GroupMembership(Guid groupId, Guid schoolId, Guid studentId, DateTimeOffset joinedAt)
    {
        Guard.Against.Default(groupId, nameof(groupId));
        Guard.Against.Default(schoolId, nameof(schoolId));
        Guard.Against.Default(studentId, nameof(studentId));
        GroupId = groupId;
        SchoolId = schoolId;
        StudentId = studentId;
        JoinedAt = joinedAt;
    }
}
```

3. Create `IGroupRepository.cs`:
```csharp
public interface IGroupRepository : IRepository<Group>
{
    Task<Group?> FindByIdAsync(Guid groupId, CancellationToken ct);
    Task<Group?> FindByIdWithMembersAsync(Guid groupId, CancellationToken ct);
    Task<List<Group>> GetBySchoolAsync(CancellationToken ct);
    void Add(Group group);
    void Remove(Group group);
}
```

4. Create `LessonSlot.cs` with `DateTimeOffset` properties and validation in constructor and `Reschedule`.

5. Create `ILessonSlotRepository.cs` with `HasConflictAsync(Guid teacherId, DateTimeOffset startTime, DateTimeOffset endTime, Guid? excludedSlotId, CancellationToken ct)`.

**EF Core configurations:**

6. Create `GroupConfiguration.cs`: table name `groups`, `Name` max length 200, `Color` max length 50, configure `_members` backing field via `builder.HasMany(g => g.Members).WithOne().HasForeignKey(m => m.GroupId)` with `PropertyAccessMode.Field`.

7. Create `GroupMembershipConfiguration.cs`: table name `group_memberships`, unique index on `(GroupId, StudentId)`, `JoinedAt` with `.HasColumnType("timestamp with time zone")`.

8. Create `LessonSlotConfiguration.cs`: table name `lesson_slots`, `StartTime` and `EndTime` with `.HasColumnType("timestamp with time zone")`. Composite index on `(TeacherId, StartTime, EndTime)`.

**Repositories:**

9. Create `GroupRepository.cs` implementing `IGroupRepository`. `FindByIdWithMembersAsync` must `.Include(g => g.Members)`. Follow `RoleRepository` pattern.

10. Create `LessonSlotRepository.cs` implementing `ILessonSlotRepository`. `HasConflictAsync` MUST use `_context.LessonSlots.IgnoreQueryFilters()` for global conflict check (per D-04). Exact overlap predicate (per D-05): `s.TeacherId == teacherId && s.Id != (excludedSlotId ?? Guid.Empty) && s.StartTime < endTime && s.EndTime > startTime` — strict inequalities, no buffer.

**Update SchedulingDbContext (from Plan 01 shell):**

11. Add DbSets: `Groups` (`DbSet<Group>`), `LessonSlots` (`DbSet<LessonSlot>`), `GroupMemberships` (`DbSet<GroupMembership>`)

12. Add HasQueryFilter calls in `OnModelCreating`:
- Group: `g => g.SchoolId == tenantContext.SchoolId && !g.IsDeleted` (per D-02)
- LessonSlot: `s => s.SchoolId == tenantContext.SchoolId` (tenant only)
- GroupMembership: `m => m.SchoolId == tenantContext.SchoolId`

13. Update `GlobalUsings.cs` to add aggregate namespace imports.
  </action>
  <verify>
    <automated>dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj --no-restore</automated>
  </verify>
  <done>Group aggregate with Name/SchoolId/MaxCapacity/Color/ISoftDelete and AddMember/RemoveMember methods exists. LessonSlot aggregate with DateTimeOffset StartTime/EndTime and Reschedule/ChangeTeacher exists. GroupMembership with denormalized SchoolId exists. All EF configs have correct column types. LessonSlotRepository.HasConflictAsync uses IgnoreQueryFilters. SchedulingDbContext has 3 HasQueryFilter calls.</done>
</task>

<task type="auto">
  <name>Task 2: Create architecture tests, unit test project, and skeleton domain tests</name>
  <files>
    tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj,
    tests/Edvantix.ArchTests/Abstractions/BaseTest.cs,
    tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs,
    tests/Edvantix.ArchTests/Domain/SchedulingTenantIsolationTests.cs,
    tests/Edvantix.Scheduling.UnitTests/Edvantix.Scheduling.UnitTests.csproj,
    tests/Edvantix.Scheduling.UnitTests/GlobalUsings.cs,
    tests/Edvantix.Scheduling.UnitTests/Domain/GroupAggregateTests.cs,
    tests/Edvantix.Scheduling.UnitTests/Domain/LessonSlotAggregateTests.cs
  </files>
  <read_first>
    tests/Edvantix.ArchTests/Abstractions/BaseTest.cs,
    tests/Edvantix.ArchTests/Abstractions/ArchUnitBaseTest.cs,
    tests/Edvantix.ArchTests/Edvantix.ArchTests.csproj,
    tests/Edvantix.ArchTests/Domain/TenantIsolationTests.cs,
    src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs
  </read_first>
  <action>
**Architecture tests:**

1. In `Edvantix.ArchTests.csproj`, add ProjectReference:
```xml
<ProjectReference Include="..\..\src\Services\Scheduling\Edvantix.Scheduling\Edvantix.Scheduling.csproj" />
```

2. In `BaseTest.cs`, add:
```csharp
using Edvantix.Scheduling;
protected static readonly Assembly SchedulingAssembly = typeof(ISchedulingApiMarker).Assembly;
```

3. In `ArchUnitBaseTest.cs`:
- Add `SchedulingAssembly` to the `.LoadAssemblies(...)` call
- Add `SchedulingServiceTypes` object provider following the existing pattern
- Add `nameof(Scheduling) => SchedulingServiceTypes` to the `GetServiceTypes` switch expression

4. Create `SchedulingTenantIsolationTests.cs` following `TenantIsolationTests.cs` pattern:
- Test method: `GivenTenantedEntities_WhenCheckingSchedulingDbContextModel_ThenAllShouldHaveQueryFilter`
- Construct `TenantContext`, build `SchedulingDbContext` with InMemory provider
- Find all entity types implementing `ITenanted`, assert each has `GetDeclaredQueryFilters().Any()`
- Verify at least 3 ITenanted entities exist (Group, LessonSlot, GroupMembership)

**Unit test project:**

5. Create `tests/Edvantix.Scheduling.UnitTests/Edvantix.Scheduling.UnitTests.csproj`:
- TUnit test framework (same packages as ArchTests or other test projects)
- ProjectReference to `src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj`
- Check existing test projects for the exact TUnit package references to use

6. Create `tests/Edvantix.Scheduling.UnitTests/GlobalUsings.cs` with TUnit + standard test usings.

7. Create `tests/Edvantix.Scheduling.UnitTests/Domain/GroupAggregateTests.cs`:
```csharp
[Test]
public void GivenValidArguments_WhenCreatingGroup_ThenPropertiesAreSet()

[Test]
public void GivenNullName_WhenCreatingGroup_ThenThrowsArgumentException()

[Test]
public void GivenDefaultSchoolId_WhenCreatingGroup_ThenThrowsArgumentException()

[Test]
public void GivenNewStudentId_WhenAddingMember_ThenMemberIsAdded()

[Test]
public void GivenExistingStudentId_WhenAddingMember_ThenMemberCountUnchanged()

[Test]
public void GivenExistingMember_WhenRemovingMember_ThenMemberIsRemoved()

[Test]
public void GivenNonExistingMember_WhenRemovingMember_ThenNoError()

[Test]
public void GivenGroup_WhenDeleting_ThenIsDeletedIsTrue()
```

8. Create `tests/Edvantix.Scheduling.UnitTests/Domain/LessonSlotAggregateTests.cs`:
```csharp
[Test]
public void GivenValidArguments_WhenCreatingLessonSlot_ThenPropertiesAreSet()

[Test]
public void GivenEndTimeBeforeStartTime_WhenCreatingLessonSlot_ThenThrowsArgumentException()

[Test]
public void GivenValidTimes_WhenRescheduling_ThenTimesAreUpdated()

[Test]
public void GivenEndTimeBeforeStartTime_WhenRescheduling_ThenThrowsArgumentException()

[Test]
public void GivenValidTeacherId_WhenChangingTeacher_ThenTeacherIdIsUpdated()

[Test]
public void GivenDefaultTeacherId_WhenChangingTeacher_ThenThrowsArgumentException()
```
  </action>
  <verify>
    <automated>dotnet test tests/Edvantix.ArchTests --filter "SchedulingTenantIsolation" --no-restore && dotnet test tests/Edvantix.Scheduling.UnitTests --no-restore</automated>
  </verify>
  <done>Architecture tests pass confirming all 3 ITenanted entities have HasQueryFilter. Unit test project Edvantix.Scheduling.UnitTests created with passing tests for Group aggregate (constructor, AddMember idempotency, RemoveMember, Delete) and LessonSlot aggregate (constructor validation, Reschedule, ChangeTeacher).</done>
</task>

</tasks>

<verification>
1. `dotnet build src/Services/Scheduling/Edvantix.Scheduling/Edvantix.Scheduling.csproj` succeeds
2. `dotnet test tests/Edvantix.ArchTests --filter "SchedulingTenantIsolation"` passes
3. `dotnet test tests/Edvantix.Scheduling.UnitTests` passes
4. `grep -r "DateTime\.Now\|DateTime\.UtcNow" src/Services/Scheduling/` returns no matches (SCH-10 compliance)
5. `grep -c "HasQueryFilter" src/Services/Scheduling/Edvantix.Scheduling/Infrastructure/SchedulingDbContext.cs` returns 3
</verification>

<success_criteria>
- Group, LessonSlot, GroupMembership domain model complete with correct interfaces
- All DateTimeOffset columns mapped to `timestamp with time zone`
- Architecture tests pass for Scheduling tenant isolation (3 ITenanted entities)
- Unit test project exists with passing aggregate behavior tests
- LessonSlotRepository uses IgnoreQueryFilters for global conflict check
- No DateTime.Now/UtcNow usage in Scheduling service code
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-slots-and-views/03-02-SUMMARY.md`
</output>
